# Sidecar runs

Raw or bulky execution evidence lives here when it is useful to preserve but should not clutter current course truth. Prefer concise durable conclusions in `../reports/` and keep this directory for receipts, logs, raw worker output, and reproducibility evidence.
